# To stop the worker:
sudo supervisorctl stop netumo-worker:*

# To start it again:
sudo supervisorctl start netumo-worker:*

# To completely remove it:
sudo supervisorctl remove netumo-worker
sudo rm /etc/supervisor/conf.d/netumo-worker.conf
sudo supervisorctl update

# Graceful stop (finishes current job):
sudo supervisorctl stop netumo-worker:*

# Immediate stop:
sudo supervisorctl signal TERM netumo-worker:*
